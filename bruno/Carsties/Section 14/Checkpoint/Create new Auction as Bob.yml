info:
  name: Create new Auction as Bob
  type: http
  seq: 2

http:
  method: POST
  url: "{{api-carsties}}/auctions"
  body:
    type: json
    data: |-
      {
        "make": "Ford",
        "model": "{{$randomBsBuzz}}",
        "color": "{{$randomColor}}",
        "year": "2020",
        "mileage": "{{$randomInt}}",
        "imageUrl": "https://cdn.pixabay.com/photo/2016/05/05/18/03/coupe-1374448_1280.jpg",
        "reservePrice": "150",
        "description": "{{$randomLoremParagraphs}}",
        "auctionEnd": "{{dateString}}"
      }
  auth:
    type: bearer
    token: "{{BobToken}}"

runtime:
  scripts:
    - type: before-request
      code: |-
        var date = new Date()
        date.setDate(date.getDate() + 14);
        var dateString = date.toISOString();
        bru.setVar("dateString", dateString);
    - type: after-response
      code: |-
        var jsonData = res.getBody();

        bru.setVar("createdItemId", jsonData.id);
    - type: tests
      code: |-
        var jsonData = res.getBody();

        test("Status code is 201 OK response", function () {
            expect(res.getStatus()).to.equal(201);
        });

        test("Response body contains Bob as seller", function () {
            expect(jsonData).to.haveOwnProperty('seller');
            expect(jsonData.seller).to.eq('bob');
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
