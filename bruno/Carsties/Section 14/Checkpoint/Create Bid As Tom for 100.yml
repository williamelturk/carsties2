info:
  name: Create Bid As Tom for 100
  type: http
  seq: 6

http:
  method: POST
  url: "{{api-carsties}}/bids?auctionId={{createdItemId}}&amount=100"
  params:
    - name: auctionId
      value: "{{createdItemId}}"
      type: query
    - name: amount
      value: "100"
      type: query
  auth:
    type: bearer
    token: "{{TomToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        var jsonData = res.getBody();

        test("Status code is 200", function () {
            expect(res.getStatus()).to.equal(200);
        });

        test("Bid status is TooLow", function () {
            expect(jsonData).to.haveOwnProperty('bidStatus');
            expect(jsonData.bidStatus).to.eq('TooLow');
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
