info:
  name: Get id-svc discovery document
  type: http
  seq: 3

http:
  method: GET
  url: "{{identitySvc}}/.well-known/openid-configuration"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
