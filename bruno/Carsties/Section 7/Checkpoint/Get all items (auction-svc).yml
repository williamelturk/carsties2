info:
  name: Get all items (auction-svc)
  type: http
  seq: 1

http:
  method: GET
  url: "{{auctionApi}}/api/auctions"

runtime:
  scripts:
    - type: tests
      code: |-
        var jsonData = res.getBody();

        test('Status code is 200', function() {
            expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
