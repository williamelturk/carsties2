info:
  name: Get User Info
  type: http
  seq: 4

http:
  method: GET
  url: "{{identitySvc}}/connect/userinfo"
  auth:
    type: bearer
    token: "{{AliceToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
